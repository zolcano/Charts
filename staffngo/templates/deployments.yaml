{{- range $name, $svc := .Values.services }}
{{- if $svc.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $name }}
  namespace: {{ $.Values.namespace }}
  labels:
    app: {{ $name }}
    chart: {{ $.Chart.Name }}-{{ $.Chart.Version }}
spec:
  replicas: {{ $svc.replicaCount }}
  selector:
    matchLabels:
      app: {{ $name }}
  template:
    metadata:
      labels:
        app: {{ $name }}
    spec:
      imagePullSecrets:
        - name: {{ $.Values.registry.secretName }}
      {{- if $.Values.redis.enabled }}
      initContainers:
        - name: wait-for-redis
          image: redis:{{ $.Values.redis.tag }}
          command:
            - sh
            - -c
            - |
              until redis-cli -c \
                -h redis-0.redis.{{ $.Values.namespace }}.svc.cluster.local \
                ping | grep -q PONG; do
                echo "Waiting for Redis cluster..."; sleep 3;
              done
      {{- end }}
      containers:
        - name: {{ $name }}
          image: "{{ $.Values.registry.server }}/{{ $svc.image }}:{{ $svc.tag }}"
          imagePullPolicy: {{ $svc.pullPolicy }}
          ports:
            - containerPort: {{ $svc.service.port }}
          envFrom:
            - secretRef:
                name: {{ $.Values.secret.name }}
          {{- if or $svc.env $svc.configKeys }}
          env:
            - name: ENABLE_INSTALLERS
              value: {{ $.Values.ENABLE_INSTALLERS | quote }}
            {{- if $svc.env }}
            {{- range $key, $value := $svc.env }}
            {{- if or (eq $key "REDIS_CONNECTION_STRING") (eq $key "REDIS_WS_CONNECTION_STRING") }}
            - name: {{ $key }}
              value: {{ printf "redis.%s.svc.cluster.local:6379" $.Values.namespace | quote}}
            {{- else}}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- end }}
            {{- end }}
            {{- if $svc.configKeys }}
            {{- range $svc.configKeys }}
            - name: {{ .envVar }}
              valueFrom:
                configMapKeyRef:
                  name: {{ $.Values.configMap.name }}
                  key: {{ .configMapKey }}
            {{- end }}
            {{- end }}
          {{- end }}
          resources:
            {{- toYaml ($svc.resources) | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $name }}
  namespace: {{ $.Values.namespace }}
  labels:
    app: {{ $name }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ $svc.service.port }}
      targetPort: {{ $svc.port }}
      protocol: TCP
  selector:
    app: {{ $name }}
{{- end }}
{{- end }}