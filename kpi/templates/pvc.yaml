---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kpi-data-pvc
  namespace: { { .Values.namespace } }
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 10Gi
