---
apiVersion: v1
kind: Service
metadata:
  name: mcp-gitlab
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.services.mcp.service.type }}
  selector:
    app: mcp-gitlab
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: http
