---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aska-data-pvc
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.services.aska.resources.storageClassName }}
  resources:
    requests:
      storage: 10Gi
