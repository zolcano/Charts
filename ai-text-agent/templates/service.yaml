---
apiVersion: v1
kind: Service
metadata:
  name: ai-text-agent
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/managed-by: Helm
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
spec:
  type: {{ .Values.services.ai.service.type }}
  selector:
    app: ai-text-agent
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
